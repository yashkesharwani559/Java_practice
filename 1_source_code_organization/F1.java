class F1 {
    int x = 20;// this is a correct statement so it will compile but gives runtime error
    // because we can do variable declaration and initialization directly in class

    // int x;
    // x=20;//this line will give compilation error
    // because java doesnot allow to initialize variable inside the class directly
}
