class M{
    final int x = 23;
    final int y;
    M(){
        y = 14;
    }
}
//this is a valid code because here we are initializing the final variable y at the time of object creation(constructor runs) and the y is a instance variable get allocated memory when its object is created and we have not initialized it before