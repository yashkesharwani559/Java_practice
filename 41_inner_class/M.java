public class M {
    // a static inner class (nested class) is not actually a static class because a class can never be static but marking the inner class static only means that the inner class can only access the static members of the class means the object of the nested class is directly linked to the class Class object of the outer class
    static class A{

    }
}
